{% extends '../base.html.twig' %}

{% block body %}
    <h1>Modele</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ modele.id }}</td>
        </tr>
        <tr>
            <th>Solde</th>
            <td>{{ modele.solde }}</td>
        </tr>
        </tbody>
    </table>

    <a class="btn btn-link" href="{{ path('modele_index') }}"><i class="fa fa-chevron-left"></i> Back to the list</a>
{% endblock %}